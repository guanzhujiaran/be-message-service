"""统一审核状态机骨架（阶段一，纯新增，不改存量行为）。

本包承载"审核/处置状态机"的**模板方法 + 显式迁移表 + 副作用钩子**，目标是把
动态 / 评论 / 私信 / 头像 / 封面等所有审核对象的流转收敛到同一套骨架
（对齐动态 `interaction_actions` 的资源化实现），并让所有流转统一写
`TResourceAuditLog` 通用审核流水。

阶段边界
--------
- 阶段一：只新增本骨架（含语义动作 / 迁移表 / 基类 / 映射器）+ 单测，
  不触碰任何存量文件与存量行为；
- 阶段二起：统一 `ResourceAuditStatusEnum`、迁移列、把存量调用切到
  `AuditStateMachine.transition()`。

本模块自包含，不 import 本包之外的业务（避免循环依赖）；被存量代码
按需接入。
"""
