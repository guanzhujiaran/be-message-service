# Moment 卡片功能后端开发计划书

> **项目**：be-message-service Moment 卡片模块
> **版本**：2.24.0
> **创建日期**：2026-08-10
> **定稿日期**：2026-08-10
> **概念命名**：为与 B 站「动态」区分、避免概念混淆，本模块业务概念统一命名为 **Moment（瞬间，类似知乎 Moment）**，下文统称 Moment。
> **状态**：开发中（迭代 2.x，已完成 2.24.0）

---

## 版本控制信息

### 版本号定义（遵循 Semantic Versioning 2.0.0）

版本号格式：`MAJOR.MINOR.PATCH`（X.Y.Z，X/Y/Z 均为非负整数，不含前导零）

| 段位 | 当前值 | 含义（严格对齐 SemVer 2.0.0） |
|---|---|---|
| MAJOR (X) | 2 | 1.0.0 定义了本计划书的**公共 API**（即已冻结的技术决策、数据库 schema、API 接口规范、开发标准）。X > 0 后，任何对公共 API 的**backward incompatible changes**必须递增 X，同时 Y、Z 归零（2.0.0 曾因概念重命名递增一次） |
| MINOR (Y) | 24 | 在**backward compatible**前提下，新增功能模块/数据表/API 端点/阶段任务，或对公共 API 功能标记 deprecated 时递增 Y，Z 归零 |
| PATCH (Z) | 0 | 仅引入**backward compatible bug fixes**（修正计划书中的错误行为/不一致，不改变范围与语义）时递增 Z |

> **0.y.z 阶段说明**：MAJOR 为 0 时表示初始开发阶段，任何内容 MAY 在任意时间变更，公共 API SHOULD NOT 被视为稳定。本计划书定稿前的 0.1.0 ~ 0.4.0 即属此阶段。
>
> **1.0.0 阶段说明**：1.0.0 标志公共 API 首次定义并冻结，后续版本号递增方式严格依本公共 API 的变更类型而定。
>
> **扩展支持**：必要时可附加 pre-release 标识（如 `1.0.0-alpha`、`1.0.0-rc.1`）与 build metadata（如 `1.0.0+20260810`），分别用 `-` 和 `+` 分隔；build metadata 不参与版本先后比较。

### 变更记录（Changelog）

| 版本 | 日期 | 变更类型 | 变更摘要 |
|---|---|---|---|
| 0.1.0 | 2026-08-10 | MINOR (0.y.z) | 初稿：基于 B站 Proto 反向推导数据模型，完成功能模块划分与分阶段计划（0.y.z 阶段，API 不视为稳定） |
| 0.2.0 | 2026-08-10 | MINOR (0.y.z) | 新增 backward compatible 功能：数据库设计（7 表 + 索引 + FK）、API 接口规范、开发规范章节 |
| 0.3.0 | 2026-08-10 | MINOR (0.y.z) | 调整计数策略：likeCount/repostCount/viewCount 统一为「明细表唯一约束幂等 + 计数原子 UPDATE ±1 + 夜间对账补偿」范式（0.y.z 阶段允许 backward incompatible 调整，按惯例递增 Y） |
| 0.4.0 | 2026-08-10 | MINOR (0.y.z) | 新增 backward compatible 规范细节：repostCount 状态机 6 触发点表、likeCount 双写链路 5 事务步骤 |
| **1.0.0** | **2026-08-10** | **MAJOR** | **定稿：公共 API 首次定义并冻结（数据模型/模块划分/分阶段计划/DB schema/API 规范/开发标准/技术决策记录全部基线化），进入开发执行阶段** |
| **1.0.1** | **2026-08-10** | **PATCH (1.0.0)** | **修正与基线决策不一致的描述（不改变范围与语义）：明确Moment 卡片全部数据表落在 be-message **MySQL 主库**（`BiliMessageDB`，由 `alembic/` 分支纳管），而非 pptr Postgres 库；相应修正 4.1/6.1/6.5 中 `JSONB`/`DateTime(timezone=True)`/`FK→TUserInfo.uid`/`alembic_pptr/` 等 PG 专属描述，改为 MySQL 兼容写法（JSON / datetime+TimestampMixin / mid 仅存 BIGINT 不建跨库 FK / alembic/ 分支）** |
| **2.0.0** | **2026-08-10** | **MAJOR (1.0.1)** | **概念重命名（backward incompatible）：将「动态(dynamic)」业务概念统一重命名为 **Moment（瞬间，类似知乎 Moment）**，与 B 站「动态」区分避免混淆。涉及：① 数据库表名 `TDynamic*` → `TMoment*`（7 张表，已重建迁移并 drop 旧表）；② 模型类 `TDynamic*` → `TMoment*`；③ 枚举 `Dynamic*Enum` → `Moment*Enum`（8 个）；④ 配置 `dyn_id_*` → `moment_id_*`、函数 `generate_dyn_id()` → `generate_moment_id()`；⑤ 文件名 `dynamic_*.py` → `moment_*.py`（含 db/schemas/api/services/tests/scripts）；⑥ 对外 API 路径前缀 `/api/v1/dynamic` → `/api/v1/moment`。**保留项**：数据库列名 `dynId`/`dynType`/`repostSrcDynId` 及对外请求/响应字段 `dynId`/`dynIdStr`/`dynType`/`srcDynId` 维持不变（避免改动表结构与 API 契约字段）；现有事件/评论系统共享枚举 `SourceTypeEnum.DYNAMIC` / `CommentTypeEnum.DYNAMIC`（值 `"dynamic"`）属存量跨模块语义，不在本次重命名范围** |
| **2.1.0** | **2026-08-10** | **MINOR (2.0.0)** | **backward compatible 新增端点**：新增 `GET /api/v1/moment/upstat`（空间统计：动态数 + 获赞数，对标 B 站 `/x/space/upstat`），属低频统计接口，不影响既有 API 契约 |
| **2.2.0** | **2026-08-10** | **MINOR (2.1.0)** | **backward compatible 新增端点**：新增 `GET /api/v1/moment/feed/following`（关注流 Feed：仅展示当前登录用户关注的人发布的 normal 动态，`FollowService.list_following_mids` 全量取关注 mid 集合过滤，复用综合页装配管线）；测试数据种子脚本改为**纯 HTTP 接口调用版** `scripts/seed_moment_via_api.py`（不直写库，走 发布→审核→点赞→评论→转发→关注 完整链路） |
| **2.3.0** | **2026-08-12** | **MINOR (2.2.0)** | **backward compatible 新增前端功能（纯前端接入，无新增后端接口）**：动态卡片与详情页接入**评论模块**（复用通用评论区 `LotteryCommentSection`，`type=DYNAMIC`、`oid=dynIdStr`，信息流点评论跳详情、详情页直接展示评论区）+ **转发模块**（新增 `MomentRepostDialog` 转发弹窗，`repostMoment` 提交转发语，成功更新 `repostCount`）；`LotteryCommentSection` 增加 `count-change` 事件用于详情页评论数联动 |
| **2.4.0** | **2026-08-13** | **MINOR (2.3.0)** | **backward compatible 评论匿名受限**：① 后端 `GET /api/v1/comment/main` 匿名访问（`viewer_mid=None`）强制 `page_size=10`（即限制最大展示 10 条，不信任前端传入的 page_size），并在响应 `data` 顶层返回 `viewer_is_anonymous: true` 标记；② `CommentListResp` 新增 `viewer_is_anonymous: bool` 字段（已登录默认 false）；③ 前端 `LotteryCommentSection` 在 `viewerIsAnonymous=true` 时，评论列表底部渲染半透明蒙层 + 「登录」按钮 + 引导文案（对标 B 站：未登录只能看前 10 条） |
| **2.5.0** | **2026-08-13** | **PATCH (2.4.0)** | **bugfix**：前端 `api/http.ts` 业务失败透传（修复网关 401 返回 `{code:-101, msg, ttl}` 无 `data` 字段被误判为服务器错误 `-1`）。优先按 `code` 字段识别业务响应（业务失败 code≠0 原样透传 code/msg），仅在 `rr` 为空（HTTP 5xx）才归为「服务器出错」 |
| **2.6.0** | **2026-08-13** | **MINOR (2.5.0)** | ① **详情页布局改造（对标 B 站）**：去除顶部「返回」header；右侧新增悬浮工具栏（点赞 / 收藏 / 转发 / 评论 + 计数，`position: fixed`，随窗口滚动）；主区域顶部 `el-tabs`（「评论」「赞与转发」两个 tab），「赞与转发」内部再分「赞」「转发」两个子 tab。② **新增后端接口**：`GET /api/v1/moment/{dynId}/likers`（点赞明细列表，按 `TMomentLike.created_at` 倒序，分页）；`GET /api/v1/moment/{dynId}/forwards`（转发该动态的列表，按 `TMoment.pubTime` 倒序，分页）；公开接口（匿名可读，与现有 detail 一致）。③ `models/schemas/moment.py` 新增 `MomentLikerItem` / `MomentForwardItem` / `MomentLikerListResp` / `MomentForwardListResp` 4 个 schema |
| **2.7.0** | **2026-08-13** | **MINOR (2.6.0)** | **昵称策略调整**：① `casdoor_service.create_local_user_from_casdoor` 创建用户时默认昵称统一改为「bili_ + uuid」格式（不再沿用 Casdoor display_name），一眼可辨为新创建账号，且天然避免 uname 重复；昵称由用户后续手动修改。② `pptr_user._upsert_profile` 用户已存在（登录/每日加经验等流程）时**不再覆盖已存在 uname**（仅空昵称时补全），登录流程传入的 Casdoor 显示名不得改动用户手动设置的昵称 |
| **2.8.0** | **2026-08-13** | **MINOR (2.7.0)** | **Casdoor 用户态查询**：`get_casdoor_user_as_user` 改为**优先用数据库 `TUserInfo.pwd` 存的 Casdoor access_token（用户模式 `Authorization: Bearer`）调 Casdoor `/api/get-user`**——能拿到 service 模式（Basic Auth）取不到的用户私有字段（积分 score / 余额等）；token 缺失或调用失败时回退原 service 模式（SDK Basic Auth）。新增 `_get_casdoor_user_by_access_token` 辅助（httpx 调用，超时 10s，非 200 / status=error 返回 None） |
| **2.9.0** | **2026-08-14** | **MINOR (2.8.0)** | **Casdoor 管理员 token 查询用户（适配「不公开账户信息」）**：Casdoor 开启「不公开账户信息」时 service 模式（clientId/clientSecret）查不到用户，改用**管理员账号**查询。① `config.py` 新增 `casdoor_admin_name` / `casdoor_admin_password` 环境变量（`CASDOOR_ADMIN_NAME` / `CASDOOR_ADMIN_PASSWORD`，docker-compose 已透传）；② 新增 `_get_admin_access_token()`：password grant（`/api/login/oauth/access_token` + `grant_type=password`）换取 admin token，进程级缓存、过期前 60s 自动刷新；③ 新增 `_get_casdoor_user_with_admin()`：用 admin token（Bearer）调 `/api/get-user?id={org}/{name}` 查询；④ `get_casdoor_user_as_user` 查询优先级：**admin 模式 → email 模式 → user_name service 模式**；⑤ 自建 `get_casdoor_user_by_user_name` / `get_casdoor_user_by_email`（httpx 直调，不依赖 casdoor SDK）。**（2026-08-16 修正实现）**：admin 登录改用**内置 application「app-built-in」**（clientId/clientSecret 与普通登录应用不同），新增 `casdoor_admin_client_id` / `casdoor_admin_client_secret`（`CASDOOR_ADMIN_CLIENT_ID` / `CASDOOR_ADMIN_CLIENT_SECRET`）、`casdoor_admin_application` 默认 `"app-built-in"`；`_get_admin_access_token()` 改为新建独立 admin SDK（`_get_admin_sdk`），直接调 SDK `get_oauth_token(username, password)` 换 token，不再手写 httpx 请求 |
| **2.10.0** | **2026-08-15** | **MINOR (2.9.0)** | **backward compatible 补齐响应字段**：动态 Feed / 详情卡片 `extend` 模块（话题）新增返回 `topicName` 字段（取自 `TMomentTopic.topicName`，装配层 `_attach_topics` 批量回填，一次 IN 查询无 N+1），前端话题卡片可直接显示话题名称并跳转话题 Feed；`MomentModule` schema 早已包含该字段，仅补装配回填逻辑，不改既有 API 契约 |
| **2.10.1** | **2026-08-15** | **PATCH (2.10.0)** | **内部维护（不改公共 API / DB schema）**：① alembic 迁移全量重建——删除历史 4 个版本，重新 `alembic init` + `autogenerate` 生成单文件 base 迁移（`20260815_1535-d2299ea47958_base.py`），`upgrade head` 重建 30 张表；② **测试数据填充重写**：Moment 系列单元测试（feed / interaction / audit / publish / topic）不再手工编造 `"seed"` 假文本，改为直连 `biliopusdb`（普通抽奖动态库，be-bilibili-crawler 维护）拉取真实动态正文 / 话题名填充（新增 `tests/seed_biliopus.py` 公共模块） |
| **2.11.0** | **2026-08-15** | **MINOR (2.10.1)** | **backward compatible 新增性能测试数据准备脚本**：新增 `scripts/seed_moment_bulk.py`（**直写 BiliMessageDB 批量灌数版**，区别于 P8-T2 的纯 HTTP 版 `seed_moment_via_api.py`）：从 `biliopusdb` 流式拉取真实动态正文 / 真实话题（`t_lot_extra_info.required_topic_text` 去重）/ 真实作者（`up_uid`）/ 真实评论与转发计数，批量灌入全 Moment 相关表（TMoment 主表 + TMomentStat 1:1 + TMomentTopic + TMomentLike + TMomentViewLog + TMomentAuditLog），默认 10 万规模（`--count` 可调、`--clean` 清空重灌、`--batch-size` 控制批量）；供 P7-T6 性能测试（Feed 分页 / 批量详情 / Stat 批量 IN 查询 EXPLAIN / 对账脚本大数据量 COUNT 验证）与开发联调使用 |
| **2.11.1** | **2026-08-15** | **PATCH (2.11.0)** | **bugfix（灌数脚本作者关联）**：`scripts/seed_moment_bulk.py` 原以外库 `biliopusdb.t_lotdyninfo.up_uid` 作为 `TMoment.mid` 灌入，这些 uid 在自有用户系统（pptr Postgres）不存在，导致 `GET /api/v1/moment/feed/all` 回查 author 用户信息不全。改为外库仅取话题与动态内容（SQL 不再 SELECT `up_uid`），作者统一链接到自有用户系统——新增 `fetch_pptr_user_pool()` 从 pptr Postgres 随机取真实用户 uid 作作者/点赞者/浏览者池，灌入时对每条动态随机映射一个本地用户为 `mid`；同时修复既有 bug：话题 `dynCount` 回填时 `session.connection()` 为协程需 `await`（原 `await session.connection().execute(...)` 报错） |
| **2.12.0** | **2026-08-15** | **MINOR (2.11.1)** | **backward compatible 新增用户空间信息接口**：新增 `GET /api/v1/user/space/info?mid=`（对标 B 站 `/x/space/wbi/acc/info`）返回单个用户完整空间资料（mid/name/sex/face/sign/level/vip/birthday/official 等，`PptrUserService.get_user_profile` 四表联查 + `is_followed` 关注态）；**不存在的用户返回专用错误码 `USER_NOT_FOUND`（而非空列表/0/404 兜底）**；**前后端黑名单空间访问拒绝**——后端在空间读接口（`/space/info` 及 `feed/space`、`upstat`、`follow/stat`）对与目标用户存在黑名单关系（`i_blocked`/`blocked_by`）的请求返回 403，前端空间页据 `/follow/relation` 的 `i_blocked`/`blocked_by` 做访问拦截与引导展示 |
| **2.13.0** | **2026-08-15** | **MINOR (2.12.0)** | **bugfix（评论计数口径）+ 卡片举报按钮统一**：① **评论计数修复**——审核状态变化（`CommentAdminService.set_state` 通过/驳回/下架）原只回写 `TMomentStat.commentCount`，**未同步评论系统 `msg_comment_subject.root_count`**，而 Feed/详情展示的 `stat.commentCount` 读的恰是 `root_count`，导致被驳回/下架的未审核评论仍计入数量；补 `root_count`/`all_count` 同步，使计数严格等于当前 `NORMAL` 评论数。② **卡片右上角举报按钮统一**——动态广场（AllFeed）/话题广场（TopicFeed）卡片与动态详情页统一显示右上角 More 菜单「举报」项（`MomentCard` 组件 `showMoreActions` 开启 + 父组件绑 `@report`，复用 `reportMoment`）；修复 TopicFeed 未绑 `@report` 的缺口 |
| **2.14.0** | **2026-08-15** | **MINOR (2.13.0)** | **统一举报系统（评论 / 动态 / 用户空间三类，RPA 独立）**：举报结构统一到 bili-common `ReportBase`（同构字段：pk/bizType/bizId/accusedMid/reportMid/reasonType/reasonDesc/pics/auditStatus/auditRemark/auditAdminMid/created_at/updated_at），三张业务表 `TMomentReport`(dynamic)/`CommentReport`(comment)/`TUserReport`(user) 继承 `ReportBase`，通用逻辑 `ReportBaseService`（record_report 幂等 / list_reports / review）在 bili-common 实现；`POST /api/v1/report`（biz_type+biz_id+reason+desc+pics）+ 管理端 list/review；动态/评论业务分开计算达阈值转审；RPA 独立；前端统一举报弹窗 `ReportDialog`（B 站风格预设原因 + 其他输入 0/60 + 图片附件）接入动态/评论/用户空间入口 |
| **2.15.0** | **2026-08-15** | **MINOR (2.14.0)** | **用户注销流程（彻底删除账号及其业务数据）**：新增 `POST /api/v1/user/deactivate`（本人注销，`CurrentUser` 校验 uid）与管理端 `POST /api/v1/user/admin/deactivate`（root/管理员注销他人）；注销后 **pptr Postgres 四表（`TUserInfo`/`TUserDetail`/`TUserLevel`/`TUserVip`）物理删除**（连同 `TUserActInfoLog`/`TUserExpRecord`/`TUserNameRecord`/`TUserPwdRecord` 关联记录）；**be-message 全部业务数据彻底清除**（关注 `msg_user_follow`、动态 `TMoment` 及 Like/View/Stat/AuditLog、评论 `msg_comment_*`、举报 `TMomentReport`/`CommentReport`/`TUserReport`、收藏 `TFavorite*`、私信 `msg_dm_*`、通知 `msg_notify_*`、事件 `msg_event_*`、设置/活跃、封禁 `msg_user_ban`、管理 `msg_admin` 等，级联批量删除）；**Casdoor 不管**（不同步禁用）；注销后 JWT 失效（复用 Redis 签名黑名单，be-gateway 撤销）；前端用户设置页新增「注销账号」入口（二次确认 + 输入密码/校验） |
| **2.15.1** | **2026-08-15** | **PATCH (2.15.0)** | **注销实现方式重构（不改公共 API / DB schema，仅内部实现）**：把「按 uid 彻底清除」的删除逻辑从 `UserDeactivateService` 单一 SQL 列表**拆分到独立可复用的领域服务**（`app/services/cleanup/` 下 `cleanup_moment`/`cleanup_comment`/`cleanup_follow`/`cleanup_report`/`cleanup_favorite`/`cleanup_dm`/`cleanup_notify`/`cleanup_event`/`cleanup_misc`/`cleanup_pptr`，各自 `delete_all_by_uid(session, uid)`）；`UserDeactivateService` 降级为**编排器**按依赖顺序依次调用；注销改为 **MQ 异步执行**——接口 `deactivate` 只校验 + 投递 `message.user.deactivate` 队列（新增 `UserDeactivatePayload`/`RK_USER_DEACTIVATE`/`user_deactivate_queue`/`publish_user_deactivate` + 消费者 `app/mq/consumers/deactivate.py`），由消费者异步跑完整删除，避免接口同步阻塞删除大用户数据的耗时；接口语义不变（本人注销 / 管理端注销他人） |
| **2.15.2** | **2026-08-16** | **PATCH (2.15.1)** | **内部维护（不改公共 API / DB schema 语义）：数据库全量重建 + seed 灌数提速**：① 修复模型索引定义 bug——`app/models/db/{moment,favorite}.py` 的 DESC 索引 `text('"createdAt" DESC')`/`text('"pubTime" DESC')` 引用带双引号的**不存在的列名**（实际列是 `created_at`/`pubTime`），MySQL 中双引号被当作字符串字面量导致 `CREATE INDEX` 语法错误，改为 `text('created_at DESC')`/`text('pubTime DESC')`；② 清空 BiliMessageDB 全部表，`alembic revision --autogenerate` 重建单一 base 迁移（`20260816_0939-e9e8e4c7b148`，30 张表一次建齐），`upgrade head` 重建成功；③ **seed 脚本批量写入提速**——`seed_moment_bulk.py` 用 `session.execute(insert(Model), dict列表)` executemany 多值 INSERT 替代 ORM `add_all` 逐行 INSERT（aiomysql 对 ORM 批量支持差），话题阶段去逐行 `flush`，10 万动态 + 27 万 Like + 83 万 ViewLog 从 ~20 分钟降至 **2 分钟**（~10 倍）；④ 修复对账脚本 `reconcile_moment_stats.py`——同 session 先跑聚合查询（返回 Row）后再 `session.exec(select(Model))` 不再映射为模型实例（`AttributeError: likeCount`），改用 `session.execute(...).scalars()`；并补 sys.path 注入支持直接运行；⑤ 修复测试清理 SQL——`test_moment_interaction.py._cleanup` 中 `DELETE FROM TMomentReport WHERE dynId=` 引用已废弃的 `dynId` 列（2.14.0 起 TMomentReport 为 ReportBase 结构），改为 `bizType='dynamic' AND bizId=`，断言同步改 `bizId`。对账后计数一致（likeCount=TMomentLike 160869、viewCount=ViewLog 287888） |
| **2.16.0** | **2026-08-16** | **MINOR (2.15.2)** | **backward compatible 用户头像修改（仅支持图片 URL + 后端下载校验）**：① bili-common `PptrUserInfoUpdateParams` 新增 `avatar` 字段（http/https，空串不修改）；② 新增 `app/services/avatar_check.py`（httpx 流式下载校验：超时 1s、≤1MB、Content-Type image/*，支持测试注入 MockTransport）；③ `/user_info/update` 非空 avatar 先校验（失败 422 具体原因）再落库（`set_user_detail(face=avatar)`）；④ 前端用户中心首页「修改头像」入口（弹窗输入链接 + 1MB/1s 提示 + 错误展示 → `UpdateUserInfo({avatar})`）；⑤ 单测 `tests/test_avatar_check.py` 7 用例（非法协议/空 URL/非图/HTTP 错误/超 1MB/超时拒绝 + 正常小图通过） |
| **2.17.0** | **2026-08-16** | **MINOR (2.16.0)** | **backward compatible 收藏 / 点赞泛化为多业务资源（bizType + bizId），动态正文支持引用任意资源**：① **交互资源类型枚举**——新增 `InteractionBizTypeEnum`（`dynamic` / `lottery` 抽奖卡片 / `rpa_action` RPA自定义操作 / `rpa_workflow` RPA工作流 / `rpa_browser` RPA浏览器实例，注册式可扩展）；② **收藏明细 `TMomentFavorite` 与点赞明细 `TMomentLike` 各新增 `bizType`/`bizId` 列**（保留 `dynId` 兼容：`bizType='dynamic'` 时 `bizId=dynId`；唯一约束分别改为 `(bizType,bizId,folderId)` / `(bizType,bizId,mid)`），`FavoriteService` / `MomentInteractionService.thumb` 泛化接收 `(biz_type, biz_id)`，资源存在性经注册式 `InteractionResourceValidator`（dynamic→`TMoment`，lottery/rpa_*→对应外部/本系统实体）校验；③ **新增通用计数表 `TInteractionStat`**（`(bizType,bizId)` 联合主键 + `likeCount` + `favoriteCount`，仅承载非动态资源的计数；动态仍用 `TMomentStat`），新增 `InteractionStatService` 原子 ±1；④ **接口扩展**——`FavoriteAddReq`/`FavoriteRemoveReq`/`FavoriteListResp`/`FavoriteDynFoldersResp` 与 `MomentThumbReq`/`MomentThumbResp` 新增可选 `bizType`（默认 `dynamic`）、`bizId` 字段（`bizType≠dynamic` 时必填）；新增 `GET /api/v1/favorite/items`（收藏夹下多类型资源明细，含资源类型 + ID 分页）与 `GET /api/v1/interaction/status`（批量查询某类型资源当前用户收藏/点赞态 + 计数）；⑤ **动态正文引用**——`MomentContentNode` 新增通用 `RESOURCE` 节点类型（`type="RESOURCE"` + `bizType` + `bizId` + `name` + `cover` 等），发布/编辑/详情装配均支持，前端按 `bizType` 渲染跳转落地页（lottery→抽奖卡片详情、rpa_*→对应 RPA 页） |
| **2.18.0** | **2026-08-16** | **MINOR (2.17.0)** | **backward compatible 社区互动统一收口：点赞/收藏/计数逻辑放入 bili-common 共享，明细存 be-message；举报保持归属各业务系统；通用枚举去重到 bili-common；新增 `rpa_plugin` 类型** |
| **2.19.0** | **2026-08-16** | **MINOR (2.18.0)** | **backward compatible 话题创建与审核（对标动态发布审核流程）**：① **`TMomentTopic` 新增审核字段**——`auditStatus`(auditing/normal/rejected)、`auditRejectReason`、`pubTime`、`creatorMid`（创建者 UID），对齐 `TMoment` 审核范式（新建→auditing 不公开展示；通过→normal+pubTime=now() 公开；驳回→rejected+写驳回原因）；② **新增用户创建话题接口** `POST /api/v1/moment/topic/create`（`CurrentUser`，校验名称唯一、名称/描述/封面合法性，创建即 `auditStatus=auditing`）；③ **新增管理端话题审核接口**（路由前缀 `/api/v1/moment/topic/audit`，root 守卫）——`GET /list`（auditing 队列分页）、`POST /approve`（→normal+pubTime=now()，**不发通知**）、`POST /reject`（→rejected+写原因，**发驳回通知给创建者**），对齐动态审核 P6 流程；④ **话题广场/Feed/热搜/详情过滤**——仅展示 `auditStatus='normal'` 且 `creatorMid` 允许的话题；`/topic/square`、`/topic/{topicId}/feed`、`/topic/hot-search` 统一加 auditStatus 过滤；⑤ **话题 `dynCount` 计数**仅统计 `auditStatus='normal'`（发布动态关联话题时校验话题必须 normal，否则 422）；⑥ 前端：话题广场新增「创建话题」入口（弹窗输入名称/描述/封面 → `createTopic`），管理后台新增「话题审核」页（root，列表/通过/驳回），个人中心展示「我创建的话题及审核状态」；⑦ **对外发布 ID 统一雪花 ID（新增规则 `snowflake-id.mdc`）**——雪花 ID 生成器下沉到 `bili-common/bili_common/core/snowflake.py`（`MinuteSnowflakeIdGenerator`），`topicId` 由自增改为**应用层雪花 ID**（`generate_topic_id()`，独立 `topic_id_worker_id`/`topic_id_epoch_sec` 配置），uid/moment_id/topic_id 数值空间分离 |
| **2.20.0** | **2026-08-16** | **MINOR (2.19.0)** | **backward compatible 抽奖卡片互动补全（点赞/收藏/转发到动态）**：① **lottery 点赞/收藏资源校验器**——`InteractionResourceValidator.register(InteractionBizTypeEnum.LOTTERY, checker)` 注册 lottery 存在性 RPC 校验器（be-message 侧新增 `LotteryRpcClient`，经 RabbitMQ RPC 弱依赖调 be-bilibili-crawler，超时/未连接/不存在返回 False），点赞 `POST /moment/thumb`、收藏 `POST /favorite/add|remove` 对 `bizType=lottery` 校验资源存在（此前默认放行）；② **lottery 转发到动态（attach 卡片）**——抽奖卡片新增「转发到动态」入口，复用 `POST /moment/create` 的 RESOURCE 节点（`bizType=lottery`、`bizId`、`name`）发布一条新动态，Feed 中渲染为可跳转抽奖详情的卡片（`MomentContentRenderer` 已支持 lottery 渲染）；发布 RESOURCE=lottery 时经 `LotteryRpcClient` 校验 lottery 存在，不存在 422；③ **前端抽奖卡片 `BiliLotteryCard` 新增点赞/收藏/转发按钮**（列表卡片 + 详情页），点赞/收藏调通用 `/moment/thumb`、`/favorite/add|remove`（bizType=lottery），互动状态/计数经 `GET /moment/interaction-status`（lottery 已支持）回显；④ **动态转发 lottery 时 Feed/详情展示卡片含互动状态与跳转**（lottery 详情页 URL 由 lottery 服务提供） |：① **通用逻辑收口 bili-common**——`InteractionBizTypeEnum`（新增 `rpa_plugin`）、`InteractionStatBase`（通用计数表字段基类）、`InteractionStatService`（非动态资源计数原子 ±1 / 批量读）、`InteractionResourceValidator`（注册式资源校验）、`str_enum_type` 等统一移到 bili-common；be-message 的 `TInteractionStat` 继承 `InteractionStatBase`，`BeMessageInteractionStatService` 绑定 `TInteractionStat` 并注册 dynamic 校验器（校验 `TMoment`）；② **点赞/收藏明细存 be-message**——`TMomentLike`/`TMomentFavorite`（2.17.0 已泛化 bizType+bizId）作为唯一明细存储，`rpa_plugin` 与 lottery/rpa_action/rpa_workflow/rpa_browser 同样走 be-message 通用点赞/收藏；③ **举报保持各业务系统**——动态/评论/用户举报在 be-message（`ReportBaseService` 通用逻辑在 bili-common），RPA 资源举报保留在 RPA-Browser（`resourcereport` 表 + `/community/*/report` + 管理端 `/reports/*`），不迁移到 be-message；④ **RPA-Browser 移除社区点赞**——删除 `resourcelike` 表、`toggle_like`/`has_liked` 方法、`/community/action|workflow|plugin/{id}/like` 三个点赞接口，RPA 资源点赞/收藏统一走 be-message；⑤ **RPC 获取资源详情**——be-message 查询互动状态时，通过 RPC 调用对应服务（RPA/lottery）获取资源详情一并返回，前端按 `bizType` 渲染落地页；⑥ **数据迁移**——RPA 库 `alembic` 删除 `resourcelike` 表（`resourcereport` 保留），be-message 库无表结构变更 |
| **2.21.0** | **2026-08-17** | **MINOR (2.20.1)** | **attach 卡片重构：独立模块 + 只存 bizId/bizType + 读取时 RPC 实时获取详情（对标 B 站 `ModuleAdditional`/`module_additional`）**：① **存储只落 bizId+bizType**——attach 卡不再写入正文 `contentJson` 的 RESOURCE 节点（不再重复保存 name/cover/jumpUrl 快照），改存 `TMoment.bizType`（VARCHAR 32）+ `TMoment.bizRid`（BIGINT）两列（复用既有预留列，**无表结构变更**）；② **发布/编辑接口新增 `attach` 字段**——`MomentCreateReq` / `MomentEditReq` 新增可选 `attach: { bizType, bizId }`（对标 B 站 `CreateCommonAttachCard { type, biz_id }`），发布时经 `InteractionResourceValidator` 校验资源存在（不存在 422）；旧版正文 `RESOURCE` 节点（2.17.0-2.20.1 存量）读取时兼容解析为独立模块；③ **Feed/详情装配为独立 `moduleType="additional"` 模块**——对标 B 站 `DynModuleType.module_additional`（`ModuleAdditional { type, rid }`），渲染于 desc 正文**下方**，不再作为正文内联节点；装配时按 `bizType` 批量经 RPC 实时获取资源详情（name/cover/jumpUrl，lottery 走 `LotteryRpcClient.get_lottery_details`，弱依赖降级仅返回 bizType+bizId）；④ **前端**：发布表单 attach 单独提交、卡片/详情渲染 `additional` 模块于正文下方（hey-api SDK 需重新生成，由用户手动同步）；⑤ **开发规范**：新增规则——社区互动接口/模型设计参考 `be-bilibili-crawler/Service/GrpcModule/Grpc/GrpcProto/bilibili` 目录下的 proto 定义 |
| **2.21.1** | **2026-08-17** | **PATCH (2.21.0)** | **bugfix（attach 不落库）**：`MomentPublishService.create` 解析出 `attach` 后调用 `_create_word` / `_create_forward` 时**未传 `attach` 参数**（两个方法签名带默认值 `None`），导致 `bizType/bizRid` 恒落库为 NULL，Feed/详情装配 `if dyn.bizType and dyn.bizRid` 恒为 False、`moduleType="additional"` 模块永不生成，前端 attach 卡不显示；修复为 `attach=attach` 显式传入 |
| **2.22.0** | **2026-08-17** | **MINOR (2.21.1)** | **动态话题多选改造：正文禁话题 + 只可选已过审话题 + 一条动态多话题（上限 5）+ extend 模块返回多话题（对标 B 站 `ModuleExtend{ repeated ModuleExtendItem }` / `EditDynRsp.topic_infos`）**：① **发布/编辑支持多话题**——`MomentCreateReq` / `MomentEditReq` 新增可选 `topics: [{topicId}]`（数组，去重，最多 5 个）；兼容保留 `topic` 单话题字段（与 `topics` 合并去重）；新增多对多关系表 `TMomentTopicRel(dynId, topicId)`（联合唯一 `(dynId,topicId)` + `idx_topic_rel_topic(topicId,dynId)` 支撑话题 Feed），`TMoment.topicId` 保留为**主话题**（= topics[0]，兼容存量数据与话题 Feed 主查询）；发布/编辑时逐一校验话题存在且 `auditStatus='normal'`（否则 400，路由层统一 ValueError→400，与 attach 校验一致）、数量 ≤ `_MAX_TOPIC_COUNT`(5)、去重；② **正文禁止 `#话题#` 文本标记**——发布文本中不允许通过 `#话题#` 添加话题（前端 `buildMomentContentNodes` 不再解析 TOPIC 节点，placeholder 移除「#话题#」提示），话题只能从**已创建且审核通过**的话题中选择；存量正文 TOPIC 节点（旧数据）读取时保持兼容渲染；③ **Feed/详情装配 extend 模块返回多话题**——`MomentModule` 新增 `topics: [{topicId, topicName}]`（保留 `topicId`/`topicName` 单话题字段兼容，= 主话题）；`_attach_topics` 批量经 `TMomentTopicRel` 回填全部话题名称（一次 IN 查询无 N+1）；`topic_feed` 查询兼容关系表 + `topicId` 主话题列；④ **前端**：发布表单话题选择器改 `el-select multiple`（`multiple-limit=5`，仅可选审核通过话题）、卡片/详情 extend 模块渲染**多个话题卡**（显示于作者信息与正文之间）（hey-api SDK 需重新生成，由用户手动同步） |
| **2.22.1** | **2026-08-17** | **MINOR (2.22.0)** | **管理员删除动态（管理端动态治理，对标 B 站审核/治理流）**：① **后端**——`MomentPublishService.remove` 新增 `operator_role` 参数（`owner` 默认=作者本人，沿用「只能删除自己的动态」校验；`admin` 跳过作者校验可删除任意动态）；软删状态机触发点 ④ 保持一致（被删 FORWARD ∧ before=normal → 源动态 `repostCount -1`），审计流水 `operatorRole=admin` + `remark='管理员删除'`（`_build_audit_log` 新增 `operator_role` 参数，默认 author）；新增路由 `POST /api/v1/moment/admin/remove`（`RootUser`，与动态审核 `moment/audit/*` 权限一致），复用 `MomentRemoveReq`/`MomentRemoveResp`；② **前端**——删除入口**下沉到复用组件 `MomentCard`**（2.22.1 修订：不在审核页单独加按钮，社区所有使用卡片的场景——综合 Feed/话题 Feed/空间/详情页——统一生效）：卡片 More 菜单删除项显示条件改为 `有效作者删除 ∨ 当前用户 is_root`（管理员对**任意**动态可见删除入口）；`MomentCard.handleRemove` 统一托管确认 + 删除：管理员身份 → `ElMessageBox.confirm`（文案「管理员删除动态？」）→ `adminRemoveMoment`；作者本人 → 沿用「确定删除这条动态吗？」→ `removeMoment`；成功后 emit `remove` 事件，父组件仅负责移除列表项/详情返回；`useRpaAdminStore` 卡片内防御性加载（store 增加 in-flight 去重，避免列表多卡片并发重复请求）；`moment-api.ts` 新增 `adminRemoveMoment` 薄封装；动态审核页 `MomentAuditListView` 不再新增删除按钮（审核页非卡片场景，保持原审核操作） |
| **2.22.2** | **2026-08-17** | **PATCH (2.22.1)** | **bugfix（作者删除入口在综合 Feed/话题 Feed/详情页永不显示）**：`MomentCard.effectiveCanRemove` 原判断 `props.canRemove !== undefined` 恒成立——Vue 3 中 `canRemove?: boolean` 未传入时默认解析为 `false`（而非 `undefined`），导致未传 `can-remove` 的场景直接返回 `false`，`item.mid === currentMid` 的作者匹配逻辑永远不执行，作者删除入口从 2.22.1（及更早版本）起仅在显式传 `can-remove=true` 的空间页显示；修复为 `if (props.canRemove) return true`（显式 true 才短路），否则回落 `item.mid === currentMid`。浏览器实测（mock 登录态 + 接口）确认综合 Feed / 空间页 / 详情页三个场景作者动态删除入口均正常显示 |
| **2.23.0** | **2026-08-17** | **MINOR (2.22.2)** | **前端下线动态编辑功能 + 修复非审核通过动态的审核状态标记**：① **编辑功能下线（前端入口移除，后端 `/edit` 接口与状态机触发点③保留）**——`MomentCard` 移除 `canEdit` prop / 「编辑」菜单项 / `handleEdit`；`MomentSpaceView` 移除 `:can-edit="isOwnSpace"`、`@edit` 监听与 `handleEdit`（原实现仅为「编辑功能开发中」占位提示）；后端 `POST /moment/edit` 与 SDK 契约**不做删除**（保留 API 完整性，未来可恢复）；② **审核状态标记修复**——`MomentCard.auditBadge` 原 `switch (auditStatus)` 匹配 `case -1/0/1`（数字），但后端 `MomentAuditStatusEnum` 为 StrEnum（`'auditing'/'normal'/'rejected'/'hidden'`），Feed/详情返回的 `auditStatus` 恒为**字符串**，导致**审核标记从未显示**（作者本人视角的「待审核/审核驳回」标签一直缺失）；修复为按字符串枚举匹配：`'auditing'`→`待审核`（warning）、`'rejected'`→`审核驳回`（danger）、`'hidden'`→`已下架`（info）、`'normal'`→无标记；作者本人空间/详情页对**非审核通过**（auditing/rejected/hidden）的动态显示对应状态标签，综合 Feed/话题 Feed 仅含 normal 动态不受影响 |
| **2.24.0** | **2026-08-17** | **MINOR (2.23.0)** | **用户空间 Tab 改为下拉菜单（纯前端）**：空间页「主页/动态/合集和系列/收藏/设置」由**顶部横排 Tab 栏**改为**下拉菜单（`el-dropdown`）**（对标 B 站空间「菜单」交互，更紧凑）：① **模板**——移除原 `moment-space__tabs` 横排 Tab 循环，改为「空间」label + `el-dropdown` 菜单：trigger 显示当前激活 Tab（`currentTab`：标题 + 图标 + 下拉箭头），下拉菜单循环渲染 `tabs` 各项（含图标，当前激活项高亮）；`@command="switchTab"` 切换 `activeTab`（纯页面内切换，URL 不变）；② **script**——新增 `currentTab` computed（`tabs.find(t.name===activeTab)`，含图标，找不到回退 dynamic）与 `switchTab(name)`；原 `currentTabTitle` 移除，占位 `EmptyState` 文本改用 `currentTab.title`；补 `ArrowDown` 图标 import、移除已下线的 `Edit` 图标 import；③ **路由不变**——仍为 `/app/space`（自己）与 `/app/space/:mid`（他人）两个路由，`activeTab` 保持组件内 `ref` 切换 |
| **2.25.0** | **2026-08-18** | **MINOR (2.24.0)** | **用户空间 UI 整改（对标 B 站空间）**：① **「请先登录」bugfix**——登录态是异步加载的（`App.vue` 在 `onMounted` 后才请求 nav），空间页 setup 阶段同步读 `user_nav.uid` 恒为空串，自己空间被误判为未登录；修复 `routeMid`/`currentMid` 改为响应式 `computed`、`activeTab` 默认值改 `resolvedActiveTab` 兜底、`onMounted` 增加「自己空间且 uid 未就绪时 watch uid 到达后再 loadFirst」逻辑；② **头像上移**——`-mt-12` → `-mt-16` 更贴近横幅；③ **导航改横向栏**——移除 2.24.0 的下拉菜单，改为 B 站风格横向导航栏（`主页/动态/合集和系列/收藏/设置` 图标+文字横排，激活项 `text-msg-link + bg-primary-light-3/50 + font-bold` 高亮，`@click` 切换 Tab）；④ **B 站 SVG 资源整理**——`src/assets/svgs/font_2_svg/` 为 B 站前端**逆向抓取**的图标（无源文件，codepoint 命名），**不作为项目正式资源**；94 个图标经脚本**转换规范格式**（根节点 `width="100%" height="100%" fill="currentColor"`、path 去 fill）并**平铺到 `src/assets/svgs/space/` 目录**（空间页专用，简化语义命名：`home.svg`/`dynamic.svg`/`favorite.svg`/`setting.svg`/`user_level_0~6(.color0/1).svg` 多色徽章等；冲突项显式区分 `tag.svg`/`tag_alt.svg`；映射表 `space/_svg_map.md`），逆向目录 `font_2_svg/` 已删除；`space/` 原 5 个手写导航图标由 B 站版替换；空间页导航由 Element 图标换为 B 站风格 SVG（`?component` import），移除对应 Element 图标 import；**根目录原 `like.svg`/`setting.svg`/`more.svg` 被同名覆盖，已用 `space/` 下语义一致 B 站规范版恢复** |
| **2.25.1** | **2026-08-18** | **PATCH (2.25.0)** | **bugfix（vite 编译 SVG 报 svgo 崩溃）**：B 站 SVG 路径含相对 `q` 曲线（`M514 55 q-2 -1...` 密集小段），`vite-svg-loader` 默认走 svgo 3.x 优化，`convertPathData.js` 的 `reflectPoint` 处理此类路径时读取 undefined 崩溃（`Cannot read properties of undefined (reading '0')`），首次编译任意 `?component` 的 B 站图标即触发 vite error overlay；修复为 `vite.config.ts` 中 `svgLoader({ svgo: false })`——**禁用 svgo 优化**（仅做组件化转换，SVG 已按规范 `width="100%"/height="100%"/fill="currentColor"` 预转换，无需 svgo），清缓存后首次编译 5 个导航图标全部 200 无报错 |
| **2.26.0** | **2026-08-18** | **MINOR (2.25.1)** | **空间页显示用户等级徽章（B 站风格 SVG）**：① 数据——`SpaceInfoResp.level`（后端 `space/info` 返回 `TUserLevel.current_level`）已存在，直接复用；② 前端——`MomentSpaceView` 用户名右侧新增等级徽章：import `space/user_level_0~6.svg`（B 站风格单色完整版），`levelBadge` computed 按 `targetUser.level`（0~6）动态选图（越界/未知返回 undefined 不显示），`el-icon` 包裹 `<component :is>` + `title="等级 Lv.N"` 悬浮提示；浏览器实测（mock level=5）徽章渲染 `title="等级 Lv.5"`；lint 0 诊断 |
| **2.26.1** | **2026-08-18** | **PATCH (2.26.0)** | **图标基础样式（对标 B 站 `vui_icon`）**：新增 `.bili-icon` 全局图标基础类（`display:inline-block; line-height:1; vertical-align:baseline; font-style:normal; font-weight:400; text-transform:none`，抄 B 站 `vui_icon` 去 `speak` 过时属性）于 `src/assets/app-tailwind.css` 的 `@layer components`；应用于空间页 SVG 图标（横向导航栏 `tab.icon`、等级徽章 `levelBadge`），消除 SVG 在文本流中的基线间距异常 |

### 版本演进规则（严格对齐 SemVer 2.0.0 第 8-10 条）

1. **MAJOR (X)**：当对公共 API 引入任何 **backward incompatible** 变更时 MUST 递增 X，且 Y、Z MUST 归零。适用情形举例：
   - 已冻结的数据表被删除或列语义发生不兼容变更
   - 已发布的 API 端点被移除或请求/响应字段语义不兼容变更
   - 审核流程、计数范式等核心决策被推翻重做
2. **MINOR (Y)**：当向公共 API 引入 **backward compatible** 的新功能时 MUST 递增 Y，且 Z MUST 归零；任何公共 API 功能被标记 deprecated 时 MUST 递增 Y。适用情形举例：
   - 新增数据表、新增 API 端点、新增阶段任务
   - 在不影响既有语义前提下扩展字段说明或新增索引
3. **PATCH (Z)**：当仅引入 **backward compatible bug fixes**（内部修改以修正错误行为）时 MUST 递增 Z。适用情形举例：
   - 修正计划书中的笔误、数值错误、引用错链
   - 修正与既有决策不一致的描述（不改变决策本身）
4. 一旦某个版本发布，该版本内容 MUST NOT 被修改；任何修改 MUST 以新版本发布。
5. 任何基线变更必须先更新本变更记录表，再修改对应章节，并同步通知评审参与方。

---

## 目录

- [版本控制信息](#版本控制信息)（本文件）
- [一、数据模型分析总结](./01-data-model.md)
- [二、功能模块划分](./02-modules.md)
- [三、分阶段开发进度安排](./03-phases.md)
- [四、数据库设计](./04-database.md)
- [五、API 接口规范](./05-api.md)
- [六、开发规范与代码质量标准](./06-dev-standards.md)
- [七、技术方案决策记录](./07-decisions.md)
- [八、任务完成记录表](./08-task-records.md)
- [九、评审与签署](./09-review.md)
- [附录：参考文件索引](./appendix.md)
